<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.melisa.rincemathew.malayalamgkquiz">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScoreActivity" />
        <activity android:name=".timerActivity" />
        <activity android:name=".setting" />
        <activity android:name=".Learn" />
        <activity android:name=".LearnKerala" />
        <activity android:name=".ScrollView" />
        <activity android:name=".allLearn" />
        <activity android:name=".LearnIndia" />
        <activity android:name=".questionanswer" />

        <service android:name=".FcmIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".Sahithyam" />
        <activity android:name=".Shastram" />
        <activity android:name=".World"></activity>
    </application>

</manifest>